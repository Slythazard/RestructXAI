[project]
name = "RestructXAI"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
  "celery>=5.6.2",
  "flask-socketio>=5.6.0",
  "logger>=1.4",
  "pika>=1.3.2",
]

[tool.uv]
environments = ["sys_platform == 'linux'"]

[tool.uv.workspace]
members = ["services/*", "libs/*"]
